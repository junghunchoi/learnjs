1. 
document.querySelector()를 통해 받아온 객체에서
classList 등을 조회할 수 있다.

2. 
문자열간 비교를 해야할 경우 문자들을 배열안에 특정 숫자로 치환한 후
비교하면 코드를 효율적으로 작성할 수 있다.

3.
간단한 논리식과 js의 기본적인 문법을  학습하는거 같음.

4.
얕은 복사를 하기 위해선 ...연산자를 사용한다.

5. 
let's get it 10장 정리

1) 버튼을 눌러 game class를  호출한다. 
- 시작할 때 생성자에 함수를 맵핑해 필요한 요소를 담는다.
- document.addEventListener('click', ~}); 에서
  콜백함수의 경우 화살표 함수일 때 this 는 window이고 일반 함수 선언문의 경우 document로 출려된다.
  이는 addEventListener가 콜백함수의 this를 event.target으로 바꿔서 호출하기 때문이다.
  따라서 클래스내에서 addEventListener에 콜백함수를 줄 경우 해당함수는
  무조건 화살표 함수로 만들어서 던져야함.

 